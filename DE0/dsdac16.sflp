
// Delta Sigma Converter

circuit dsdac16
{
	input din<16>;
	output dout;

	reg_wr sigma<18>; // +2
	sel delta<18>; // +2

	delta = sigma<17> || sigma<17> || din;
	sigma += delta;
	dout = sigma<17>;
}
